# OpenManusWeb 项目结构文档

本文档是 OpenManusWeb 项目结构的主索引。

---

## 项目概览

OpenManusWeb 是一个完全前端驱动的 AI Agent 项目，使用 WebLLM 在浏览器中运行大语言模型，无需服务器资源。

**核心特性：**

- 🚀 完全前端运行，无需后端服务器
- 🧠 使用 WebLLM 在浏览器中运行 Qwen3-4B 模型
- 🤖 Agent 核心引擎：规划 → 执行 → 反思
- 🔧 MCP（Model Context Protocol）工具集成
- ⚡ React 19 + Vite 7 + TypeScript 5.9
- 🎨 Tailwind CSS + shadcn/ui 设计系统
- 📦 MobX 状态管理
- 🏗️ Monorepo 架构（pnpm workspaces）

---

## 根目录结构

```
OpenManusWeb/
├── .git/                          # Git 版本控制
├── .idea/                         # IDE 配置（WebStorm/IntelliJ）
├── docs/                          # 项目文档
│   ├── rules/                     # 编码规范（拆分为多个子文档）
│   │   ├── CODING_STANDARDS.md    # 主索引
│   │   ├── naming-and-style.md    # 命名与代码风格
│   │   ├── components-and-state.md # 组件与状态管理
│   │   ├── styling-and-typescript.md # 样式与 TypeScript
│   │   ├── development-workflow.md # 开发流程
│   │   └── agent-development.md   # Agent 开发规范
│   ├── structure/                 # 结构说明（拆分为多个子文档）
│   │   ├── agent-core.md          # agent-core 核心架构
│   │   ├── web-app.md             # web-app 目录结构
│   │   ├── configuration.md       # 构建配置与依赖
│   │   └── development.md         # 开发工作流
│   └── PROJECT_STRUCTURE.md       # 本文档
├── packages/                      # Monorepo 包目录
│   ├── agent-core/               # Agent 核心引擎
│   ├── mcp-bridge-extension/     # MCP 浏览器扩展桥接
│   └── web-app/                  # React 前端应用
├── .mcp.json                      # MCP 服务器配置
├── .editorconfig                  # 编辑器配置
├── .prettierrc                    # Prettier 配置
├── eslint.config.js               # ESLint 配置（根）
├── package.json                   # 根项目配置
├── pnpm-workspace.yaml            # pnpm 工作空间配置
├── pnpm-lock.yaml                 # 依赖锁定文件
└── README.md                      # 项目说明
```

---

## 核心架构

### 三层架构

```
┌─────────────────────────────────────────────────────────┐
│                      UI 层 (web-app)                     │
│   展示：消息、Plan结构、思考过程、执行状态、最终结果      │
├─────────────────────────────────────────────────────────┤
│                   Agent 层 (agent-core)                  │
│   规划 → 执行 → 反思                                     │
│   暴露给UI层：工具执行结果、step完成状态、思考/反思过程   │
├─────────────────────────────────────────────────────────┤
│                    工具层 (MCP + 内置工具)                │
│   LLM生成、JS执行、MCP远程工具                           │
└─────────────────────────────────────────────────────────┘
```

### 数据流

```
用户输入
    ↓
UI层 (InputStore) ──────→ Agent层 (Agent.task)
                               ↓
                         PlanAndRethink.plan()
                               ↓
                    ┌──────────┴──────────┐
                    ↓                     ↓
             LLM 规划生成            解析 PlanSchema
                    └──────────┬──────────┘
                               ↓
                    遍历执行 steps
                    ├─ toolLLM.reload()
                    ├─ tool.step(step_goal)
                    │   ├─ think: LLM决策工具调用
                    │   └─ act: 执行工具
                    └─ toolLLM.unload()
                               ↓
                    返回结果到UI层
                               ↓
              UI更新 (messages, plan状态)
```

---

## Packages 说明

### 📦 [agent-core](./structure/agent-core.md)

Agent 核心引擎，处理智能体的规划/执行/反思等核心逻辑。

**核心模块：**

- `agent.ts` - Agent 主类入口
- `planAndRethink/` - 规划与反思模块
- `llm/` - LLM 引擎封装
- `tools/` - 工具框架
- `mcp/` - MCP 协议集成

**暴露给 UI 层的信息：**

- 工具执行结果
- Plan 结构（tasks, steps）
- 思考过程（think）
- 反思过程（rethink）
- 每个 step 的完成状态
- 最终任务结果

### 📦 mcp-bridge-extension

浏览器扩展，用于解决 CORS 问题，作为 MCP 请求的代理桥接。

**核心文件：**

- `background.ts` - 扩展后台脚本
- `content.ts` - 内容脚本
- `page-bridge.ts` - 页面桥接通信

### 📦 [web-app](./structure/web-app.md)

React 前端应用，提供用户交互界面。

**核心功能：**

- 消息列表展示
- Plan 结构可视化
- 思考/反思过程展示
- 工具调用状态显示
- 主题切换

---

## 结构文档索引

### 🤖 [agent-core 核心架构](./structure/agent-core.md)

详细说明 Agent 核心引擎的架构：

- Agent 主类与生命周期
- 规划与反思模块（PlanAndRethink）
- LLM 模块（WebLLM 封装）
- 工具框架（ToolBase, ToolCall）
- MCP 集成（Client, Host, Bridge）
- 与 UI 层的数据协议

### 📁 [packages/web-app 目录结构](./structure/web-app.md)

详细说明 web-app 包的目录结构：

- 完整目录树
- `src/components/` - 可复用组件
- `src/pages/` - 页面组件
- `src/stores/` - MobX 状态管理（与 Agent 集成）
- `src/lib/` - 工具函数
- 配置文件说明

### ⚙️ [构建配置与依赖](./structure/configuration.md)

详细说明构建配置和依赖包：

- Vite 配置
- Tailwind CSS 配置
- TypeScript 配置
- shadcn/ui 配置
- 核心依赖说明
- 开发依赖说明

### 🛠️ [开发工作流与扩展指南](./structure/development.md)

详细说明开发流程和功能扩展：

- 路由结构
- 命名约定
- 开发命令（dev、build、lint）
- 添加新页面
- 添加新 Store
- 添加新组件
- 调试技巧

---

## 快速参考

### 目录组织

```
packages/
├── agent-core/           # Agent 核心引擎
│   └── src/
│       ├── agent.ts      # Agent 主类
│       ├── llm/          # LLM 模块
│       ├── planAndRethink/ # 规划与反思
│       ├── tools/        # 工具框架
│       ├── mcp/          # MCP 集成
│       └── types/        # 类型定义
├── mcp-bridge-extension/ # MCP 浏览器扩展
└── web-app/              # React 前端
    └── src/
        ├── components/   # 可复用组件
        ├── pages/        # 页面组件
        ├── stores/       # MobX 状态管理
        ├── lib/          # 工具函数
        ├── app.tsx       # 路由配置
        ├── main.tsx      # 应用入口
        └── index.css     # 全局样式
```

### 文件命名

```
✅ 所有文件使用 camelCase
- layout.tsx      # 组件文件
- home.tsx        # 页面文件
- themeStore.ts   # Store 文件
- utils.ts        # 工具文件
- toolCall.ts     # Agent 核心文件
```

### 导入路径

```typescript
// 使用 @/ 别名
import Layout from '@/components/layout/layout';
import { useStore } from '@/stores/rootStore';
import { cn } from '@/lib/utils';
```

### 路由结构

```
/         → Home (产品介绍)
/chat     → Chat (LLM 聊天 + Agent 交互)
```

---

## 技术栈

### 核心框架

- React 19
- TypeScript 5.9
- Vite 7
- React Router Dom 7

### 状态管理

- MobX 6
- mobx-react-lite

### UI 和样式

- Tailwind CSS 4（唯一样式方案，CSS-first 配置）
- shadcn/ui
- prompt-kit（AI 聊天 UI 组件）
- lucide-react

### AI 功能

- @mlc-ai/web-llm
- Qwen3-4B-q4f16_1-MLC 模型

### Agent 核心

- PlanAndRethink 规划引擎
- MCP（Model Context Protocol）
- 工具调用框架（ToolBase, ToolCall）

---

## 相关文档

### 编码规范

- [编码规范主页](./rules/CODING_STANDARDS.md)
- [文件命名规范与代码风格](./rules/naming-and-style.md)
- [组件规范与状态管理](./rules/components-and-state.md)
- [样式规范与 TypeScript](./rules/styling-and-typescript.md)
- [开发流程与工作规范](./rules/development-workflow.md)
- [Agent 开发规范](./rules/agent-development.md)

### 外部资源

- [React 文档](https://react.dev/)
- [MobX 文档](https://mobx.js.org/)
- [Tailwind CSS 文档](https://tailwindcss.com/)
- [shadcn/ui 文档](https://ui.shadcn.com/)
- [Vite 文档](https://vitejs.dev/)
- [WebLLM 文档](https://github.com/mlc-ai/web-llm)
- [MCP 规范](https://modelcontextprotocol.io/)

---

## 更新日志

### 2026-01-21

- ✅ 添加 agent-core 包架构文档
- ✅ 添加 MCP 集成说明
- ✅ 更新三层架构图
- ✅ 添加 Agent 开发规范
- ✅ 更新 AI 模型为 Qwen3-4B

### 2026-01-13

- ✅ 拆分文档为多个子文档（防止上下文过长）
- ✅ 统一所有文件使用 camelCase 命名（小写开头驼峰）
- ✅ 更新所有代码示例和目录树以反映新的命名约定
- ✅ 明确说明：组件函数名使用 PascalCase，文件名使用 camelCase
- ✅ 完善文档结构，添加快速参考部分

### 初始版本

- ✅ 建立项目结构文档
- ✅ 详细说明目录组织
- ✅ 提供配置文件说明
